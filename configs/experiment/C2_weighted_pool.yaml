# C2: Weighted pooling only (no gating)
# Goal: isolate attention-weighted pooling
# Expected: good performance, shows that pooling is crucial

model:
  map_guided: true
  map_guided_type: weighted_pool
  lambda_token: 0

loss:
  clip_weight: 1.0

train:
  experiment: C2_weighted_pool
