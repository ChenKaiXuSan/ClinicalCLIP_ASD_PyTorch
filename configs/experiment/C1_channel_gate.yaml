# C1: Channel gating only
# Goal: isolate channel-only gating effect
# Expected: lower than spatiotemporal due to lack of spatial modeling

model:
  map_guided: true
  map_guided_type: channel
  lambda_token: 0

loss:
  clip_weight: 1.0
